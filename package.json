{
  "name": "purescript-polyphonic-soundfonts",
  "devDependencies": {
    "bower": "^1.8.0",
    "pulp": "^11.0.2",
    "purescript": "^0.11.6"
  },
  "scripts": {
    "prebuild": "bower install",
    "build": "pulp build",
    "build-example": "pulp build --src-path example/src --include src --to example/dist/example.js"
  }
}
